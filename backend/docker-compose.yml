version: '3'
services:
  database:
    image: 'postgres:9-alpine'
    environment:
      - POSTGRES_DB=agenda
      - POSTGRES_USER=agenda
      - POSTGRES_PASSWORD=agenda
    ports:
      - '5432:5432'
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
